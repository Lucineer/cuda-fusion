[package]
name = "cuda-fusion"
version = "0.1.0"
edition = "2021"
description = "Multi-sensor data fusion — combine readings from multiple sources with uncertainty propagation"

[dependencies]
serde = { version = "1", features = ["derive"] }
